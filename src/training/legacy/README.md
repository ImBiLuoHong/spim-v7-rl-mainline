# Legacy Training Paths

These files were archived during the training mainline closure.

- `runner_env_legacy.py`
- `runner_cfg_legacy.py`
- `model_builder_legacy.py`

They are retained only for historical reference.
They are not part of the active training system.
The unique official training entry is:

```bash
python src/scripts/train_phase4_end2end.py --config <yaml>
```
